/**
 * 三端共享常量（main / preload / renderer 经 `@common/constants` 引用）。
 *
 * 只放「多处必须一致」的口径值：这类数值一旦各端各写一份，迟早会漂移。
 */

/**
 * 日志正文保留窗口（小时）：`request_body` / `response_body` 只留最近这段时间，更早的由 main 侧置空。
 *
 * 正文是日志体积的绝对大头（流式响应会把整段 SSE 文本落库），行本身（元数据）仍按 7 天保留，
 * 列表、筛选项、模型速度与来源统计都只读元数据列，因此不受清空影响。
 * 清扫在日切后的第一次写日志时执行，故实际保留区间是 12~36 小时（见 docs/data/01-数据模型.md）。
 * 渲染层用同一常量判断详情页是否该提示「正文已超保留期」。
 */
export const LOG_BODY_RETENTION_HOURS = 12
