# docs/ 文档索引

> 本目录存放项目技术文档，供后续 AI 参考。 **请先读本文件**，按功能定位目标文档，无需逐个查看文件名。

## 索引

### app/ —— 应用外壳与工程基线

| 文档                                             | 描述                                                                                                             |
|--------------------------------------------------|------------------------------------------------------------------------------------------------------------------|
| [01-init.md](./app/01-init.md)                   | 项目初始化（2026-09-15）：参考 mistrelle 搭建 electron-vite 骨架（main/preload/renderer/common 四段 + windows 多窗口模式）、TDesign + UnoCSS + 自动导入 + Pinia + Vue Router、drizzle + better-sqlite3 数据库基线、db:ping 全链路示例、各配置文件要点与别名约定；macOS Dock 跟随主窗口可见性（2026-09-16） |
| [02-管理界面.md](./app/02-管理界面.md)           | 管理界面（2026-09-15）：侧边栏布局（t-menu 路由导航）、7 个页面职责与数据来源（首页统计看板 + 用量明细表 + 日志页条件分页/行展开详情/进行中实时展示）、3 组命令式弹窗两文件模式、通用组件目录（PageLayout / EChart / usage）、server:status 状态推送链路、日志页 useLogRefresh 三路刷新（log:changed 推送 + 30s 轮询兜底 + 手动）、设置页四卡片（外观/看板卡片/开机自启/网络代理）；**提供商与模型映射归档交互（2026-09-18）**：已归档项默认隐藏 + 「显示已归档」开关、归档行置灰只留「恢复」、提供商已归档的行提示「请先恢复提供商」；**概览页取数时机与刷新节拍（2026-09-19）**：router-view 的 keep-alive 只缓存概览页、30 秒延时器节拍（进页按陈旧度补一次 + 按完成时刻重排 + 切走停表 + 窗口隐藏跳过）、一次刷新取四份数与静默刷新 |
| [03-托盘.md](./app/03-托盘.md)                   | 系统托盘（2026-09-16）：registerAppTray/refreshTrayUsage 双通道刷新（写库即时 + 30s 兜底，**兜底周期 2026-09-23 迁入 scheduler 的 `tray:usage` 任务，见 13**）、统计面板弹窗（trayPanel 窗口定位/失焦隐藏/点击竞态抑制）、click 与 right-click 平台分支（macOS 不可同时 setContextMenu）、macOS 标题显示今日用量；**面板亚克力外观（2026-09-17）**：原生材质（darwin vibrancy / win32 acrylic）+ 渲染层面板底与卡片底两层半透明叠加、`--fluent-card-bg` 作用域重定向与渐晕 `--metric-tint-alpha` 旋钮、三条已知限制（系统主题跟随 / linux 无模糊 / Win10 无材质）；**分段控件与加载态修订（2026-09-17）**：维度切换改 `variant="default-filled"` + `.fluent-segmented`（半透明轨道 + 玻璃缩略块）、`t-loading` 加 delay 且紧凑模式不盖遮罩；**两页分段切换（2026-09-23）**：标题栏改「用量统计 / 额度」分段控件、运行状态胶囊移到右侧（选择记 `localStorage['tray.tab']`），额度页见 14 |
| [04-统计看板.md](./app/04-统计看板.md)           | 统计看板（2026-09-16）：用量聚合重构（usage_daily 增加供应商/缓存/成功失败/耗时维度 + usage_hourly 小时表）、统一写入路径 recordRequest、统计口径权威定义表、时间维度解析与活跃度窗口、**模型速度折线（近七天，实时聚合 request_logs 的统计项之一）**——为什么不用聚合表（duration_ms 含失败请求而 token 只算成功，口径错配）、速度口径与 Top 5、四条已知限制（上游未返回 usage 即无数据点、端到端耗时含预填充、窗口滚动、仅成功请求）、echarts 图表基元（按需注册 + token 取色）、共享看板组件、首页与托盘面板双端结构；**维度候选恢复四档（2026-09-18）**：今天 / 近24小时 / 近七天 / 近30天（默认仍近 24 小时，两端共用 `UsageDashboard` 默认候选）；**来源 Agent 请求数（2026-09-18）**：首页新增「来源 Agent 请求数（近七天）」条形图，来源取 user-agent 归一出的客户端名称（未携带 UA 归「未知」），与速度折线同属实时聚合 request_logs、窗口固定近七天且不随维度与筛选变化（为何不落聚合表、三条已知限制、前 6 名 + 其他）；**取数归属与节拍（2026-09-19）**：模型速度与来源客户端的数据由首页按同一节拍统一取（两张卡片改为纯展示 `data` prop）；**展示层优化（2026-09-19）**：活跃度热力图固定一年（GitHub 提交图式、今天为最后一格、月份标签行，托盘 compact 切近 12 周并重算指标，`activityDaysOf` 随之删除）、数值轴刻度大数简写（复用 formatTokens，`withCompactLabel`）、供应商名超 96px 截断省略（`buildTopBarOption` 类目轴，tooltip 看全名）；**卡片可配置（2026-09-19）**：看板改注册表驱动渲染、双端独立的卡片显示/顺序配置，详见 `12-看板卡片配置.md`；**可视化升级（2026-09-20）**：活跃度热力图由 CSS Grid 手写改为 echarts 日历热力图（`optionsFlow.ts`，visualMap 色阶 + tooltip 明细 + 自适应卡宽）、新增首页专属「Agent 与提供商流向」三段式桑基图（来源 Agent → 本地路由 → 提供商，权重为请求数含失败，固定近七天，新增 `usage:agentFlow` 通道与 `clientRepo.queryAgentProviderFlow()`） |
| [05-日志代码查看器.md](./app/05-日志代码查看器.md) | 日志代码查看器（2026-09-16）：行展开正文/错误信息改用 Monaco 只读渲染，替换 v-html 与 break-all 大文本方案；动态加载、入口必须用含 contributions 的 root `monaco-editor`（`editor.api` 会报 UNKNOWN service actionWidgetService）、`exports` 映射下的 worker 路径、colors 需带 `#` 而 token rules.foreground 不能带 `#`、tdesign token 主题、空值/高度/多实例销毁约定；**正文超保留期提示（2026-09-19）**：已结束且两侧正文全空时插一条 `t-alert`，窗口常量取自 `@common/constants` |
| [06-应用图标.md](./app/06-应用图标.md)           | 应用图标（2026-09-16）：图标落点与分工（build 的 png/icns/ico 为不透明满底应用图标；resources/icon.png 为透明底托盘专用机器人图）、尺寸与用途、换图设计约束（托盘图需裁掉留白）、用系统 sips + iconutil + Node 打包 ICO 与用 Pillow 生成托盘图的完整步骤 |
| [07-开机自启.md](./app/07-开机自启.md)           | 开机自启（2026-09-16）：以系统登录项为唯一权威不落库、未打包禁用以防污染、macOS/Windows 平台差异（macOS 无 args 透传故用 wasOpenedAtLogin、Windows 读写需同 path/args）、登录启动静默驻留托盘（不建窗 + 隐藏 Dock）、app:* IPC 契约、second-instance 唤窗配套 |
| [08-服务页.md](./app/08-服务页.md)               | 服务页重构（2026-09-19）：「连接凭据优先」两张 Fluent 卡（连接卡 = 状态行 + Base URL + API Key + 端口四个 field-row；接入示例卡 = 三协议分段控件），示例数据驱动且按协议给各自实际应填的 Base URL（Anthropic 不带 /v1）、Codex 改 wire_api="responses"、状态点色阶取色与暗色覆盖、共享 utils/clipboard.ts |
| [09-看板卡片规范.md](./app/09-看板卡片规范.md)   | 看板卡片规范（2026-09-16）：MetricCard 四段解剖（图标+标签+状态胶囊 / 大数值+小单位 / 卡内迷你可视化 / 页脚明细）、卡片底色反转为「比面板底更亮」的浮起层级、迷你图只用真实数据、useUsageCards 配置派生、胶囊文案表、ServiceStatusBar 补齐托盘缺失的服务状态、新指标接入步骤；**卡片底 token 化与半透明适配（2026-09-17）**：`--fluent-card-bg`（缺省值与内容容器合成结果一致，窗口可整体重定向为玻璃底）+ 分档渐晕的 `--metric-tint-alpha` 透明度旋钮；**顶部分段控件修订（2026-09-17）**：维度切换 outline → default-filled + `.fluent-segmented` 原语（轨道半透明、缩略块用卡片底、滑动由组件自带、inset 阴影描边的原因）、紧凑模式等宽铺满、加载态 delay / 去遮罩；**上下对齐（2026-09-19）**：`.metric-card` 纵向 flex + `.metric-value` 挂 `margin-top: auto`，四卡标题钉顶、「数值+可视化+页脚」沉底（迷你柱 28px 与迷你进度条 6px 高度差不再造成页脚参差，规则写在原语上、自然高度卡不受影响）；**图表卡新壳（2026-09-20）**：活跃度热力图改 echarts 日历热力图（`UsageHeatmap.vue` 退化为薄壳），新增桑基图卡（`UsageSankeyCard.vue`） |
| [10-内置提供商.md](./app/10-内置提供商.md)       | 内置提供商（2026-09-19）：`providers.kind` 类型列（可空，null = 自定义，`ProviderKind` 三端枚举 + 0005 迁移）、新增/编辑**抽屉**（DrawerPlugin，保存/取消置底外壳 footer 不随内容滚动，取代弹窗为余量查询扩展预留宽度）**CC Switch 式「预设供应商」标签墙快速填写**（`presets.ts` 目录五家 Z.ai / OpenCode Zen / DeepSeek / OpenAI / SiliconFlow：选中回填协议与官方端点、**Base URL 字段按需隐藏**（预设端点以提示行展示）、名称未手改则跟随、切回自定义保留字段、API Key 带「获取 API Key」官网链接走 openExternal）、列表名称列类型标签、新增预设三步、**行内启停整行回传必须带 kind/quotaStrategyId/strategyConfig 否则被 update 抹成 null** 的坑；余量查询已落地为独立的策略绑定（`quotaStrategyId`，选预设时同名内置策略自动绑定），见 11 |
| [11-余量查询.md](./app/11-余量查询.md)           | 余量查询（2026-09-19）：**策略模式**——内置策略（移植 CodexBar，**2026-09-20 起全量 35 家 native TS**：Swift 移植 15 家 + JS 插件 1:1 迁移 20 家，`scripted.ts`/`builtin-plugins/` 已删除，eval 运行时保留服务外置插件，代码内置不可卸载）与外置策略（用户 `defineProvider` JS 脚本，eval 加载、可卸载可删除）统一注册内存 Map；提供商 `quota_strategy_id` 绑定 + `strategy_config` 附加配置（cookie/token/区域）；脚本宿主运行时（origin 白名单 + auth 宿主附加 + 5MB 上限 + 20s 超时 + 失败八分类中文映射；迁移映射规则与有意修正清单见文档）；`quota_snapshots` 快照 UPSERT（失败保留旧值）、**装载 `initQuota()` + 刷新节奏由 scheduler 的 `quota:refresh` 任务驱动（启动 5s 首刷 + 每 10 分钟，2026-09-23 从 `quota/scheduler.ts` 迁入，见 13-定时任务.md）**；余量页卡片（窗口进度/重置倒计时/余额/明细）、策略目录弹窗与策略编辑抽屉、提供商抽屉策略绑定；**卡片面板重塑（2026-09-19）**：对齐 CodexBar 面板视觉——头部相对时间 + 摘要、窗口行「剩余%」+ 带刻度分段进度条（`QuotaWindowRow`）、`details` 分区发丝分隔 + 双列明细、`details[].chart` 柱图渲染（`QuotaDetailChart`，契约补 `QuotaDetailChart` 类型，数据早已透传）；**eval 无沙箱为已知取舍**；**附加配置动态表单（2026-09-20）**：契约 `QuotaStrategySetting`（插件 settings[] 扩 widget/options/placeholder/hint + native meta.settings 声明，`quota:strategies` 原样透传零新增 IPC）、ProviderContent 有声明渲染 `StrategyConfigForm` 动态表单替换裸 JSON（select/密码框/textarea/hint）、提交丢弃空白值并保留声明外未知键、解析统一 `@common/utils/quotaConfig.parseQuotaConfig` |
| [12-看板卡片配置.md](./app/12-看板卡片配置.md)   | 看板卡片可配置（2026-09-19）：首页/托盘看板全部卡片支持**按界面独立配置显示与顺序**——`cardRegistry.ts` 注册表为卡片清单唯一事实源（新增卡片 = 写组件 + 注册一行，配置/编辑器/同步零改动）、看板改注册表驱动单 grid 渲染（卡片统一 `ctx` props 契约 + `span` 栅格跨度，原 `UsageRatioCard` 拆为两张独立环形卡，`showSpeed`/`showClients` props 删除改注册表 `surfaces` 约束）、**配置入口在设置页「看板卡片」卡**（首页/托盘分段 + sortablejs 拖拽 + 开关，即改即存；看板为纯展示面不放编辑元素）、布局存 settings 单键 `dashboard.layout`（每界面一份有序列表；未知 id 丢弃、新增卡按注册表顺序追加——版本升级安全；隐藏卡保留位置）、**setting 域通用偏好通道**（`setting:get/set` + `setting:changed` 全窗口广播跨窗口实时同步，后续 UI 偏好可复用）；**卡片清单扩容（2026-09-20）**：新增首页专属 `agent-provider-flow`（Agent 与提供商流向桑基图），ctx 契约补 `agentFlow`，共 15 张卡 |
| [13-定时任务.md](./app/13-定时任务.md)          | 定时任务（2026-09-23）：**main 进程全部周期任务收敛到 `src/main/src/scheduler/`**，业务文件不再自己 `setInterval`——选型 **croner**（零依赖 + 自带 TS 类型、6 段秒级 cron、`protect` 跳过重叠触发、`unref` 不阻塞进程退出；对比 node-cron v4 / toad-scheduler 的取舍）；任务契约（`id` / `cron` / `startupDelayMs` / `run`）+ `registerJob` 逐个容错；五个任务（`log:retention` 每日 04:30 行 7 天 + 正文 12 小时、`usage:retention` 每日 04:40、`db:wal-checkpoint` 每日 04:10 且启动 30s 补跑、`quota:refresh` 每 10 分钟 + 启动 5s、`tray:usage` 每 30 秒兜底）；生命周期（whenReady 链顺序、`markPendingInterrupted` 启动收口、`will-quit` 停表）；已知限制（机器唤醒不补跑、退出不等待在跑任务、checkpoint 的 busy 语义、正文实留窗口由 12~36 收紧为 12~24 小时、周期不可配） |
| [14-托盘额度面板.md](./app/14-托盘额度面板.md)   | 托盘额度面板（2026-09-23）：托盘弹出面板改**两页分段切换**（用量统计 / 额度，标题栏 `t-radio-group` + `.fluent-segmented`、运行状态胶囊移到右侧、选择记 `localStorage['tray.tab']`）——额度页按设置页配置的顺序与开关渲染各提供商余量快照卡（**每提供商整行一张卡，卡内逐窗口一行进度条**：opencode/zai 一卡 3 行，复用 `components/quota/QuotaWindowRow.vue`（2026-09-23 从余量页上移到通用目录），纯余额型退化为 `t-statistic` 大数值余额卡；`MetricCard` 外壳 + 玻璃底 + 10px 间距）；**分档色复用用量统计卡那套 `stat-*`**（剩余 ≤10% 红 / ≤30% 黄 / 其余绿，余额低于阈值红；右上角同色渐晕），阈值唯一事实源抽到 `@common/utils/quotaDisplay`（余量页 `QuotaWindowRow` 进度条改用同一函数）；配置存 settings 单键 `tray.quota`（有序 `items[{providerId, visible}]` + `balanceAlertThreshold` 默认 10；**键用 providerId** 因名字可改可重名，合并规则同看板卡片），设置页新增「托盘额度」卡（sortablejs 拖拽 + 显示开关 + 恢复默认顺序 + 阈值 `t-input-number`，即改即存 + 广播同步）；刷新策略 = 弹出读库不出站 + 「从未查到结果」的项补查（会话内每提供商一次）+ **`useIntervalFn` 每 60 秒重读快照并驱动「更新于 X 分钟前」文案**（后台每 10 分钟刷一轮，页面自己跟读）+ 手动全量刷新；两页 `v-if` 切换（补查只在切到额度页时发生）；**零新增 IPC / 零数据库迁移** |

| [15-负载均衡与故障转移.md](./app/15-负载均衡与故障转移.md) | 负载均衡与故障转移（2026-09-23）：**核心引擎**——同一对外模型名对应多家渠道（`models` 表沿用不拆，**同名多行即一个渠道组**，0007 迁移只去掉 `public_name` 唯一约束并加 `idx_models_public` + `unique(public_name, provider_id)`），可用度 0~100 即路由权重（**失败先大后小扣 / 成功后先小后大涨 / 额度耗尽直接归零 / 额度恢复直接拉满**，永不归零 + 30s 冷却探针解决「低可用度渠道拿不到流量就无法自愈」的死锁）、失败归类表（服务/凭据/额度类扣分并改道，请求类不扣不改道）、会话亲和（请求头 → 协议元数据 → 首条消息指纹三段兜底，软绑定 + 失败改道后重绑，**绑定键含对外名、优先于探针**，主进程 `[balancer]` 每请求一行带会话键——绑定表只在内存、日志页看不到会话标识）、**只在未向客户端写出任何字节时改道**（透传的非 2xx 改先缓冲、流式响应头推迟到首块到达、转换沿用惰性响应头）、一次请求只落一条日志 + `request_logs.retry_trace` 重试轨迹、首字节超时 60s（拿到响应头即清除）、额度前置拦截（余量快照 ≥100% 或余额 ≤0 → 可用度归零，`resetsAt` 到点惰性解除，启动用落库快照重建，全阻断回 503）、组级重命名/启停/归档与 `balancer:states` 健康快照、模型路由页**表格 / 链路双视图**（链路视图把内存里的路由状态画出来：对外模型名 → 各渠道支线，支线标 `可用度 + 推算占比`，占比口径 = `routeShares`；零新增 IPC、纯 CSS 折线不手写 SVG、引擎关闭时给警示、不标「首选渠道」因顺序每请求重掷） |

### data/ —— 数据模型

| 文档                                             | 描述                                                                                                             |
|--------------------------------------------------|------------------------------------------------------------------------------------------------------------------|
| [01-数据模型.md](./data/01-数据模型.md)          | 8 张表结构（providers/models/settings/request_logs/usage_daily/usage_hourly/quota_plugins/quota_snapshots）、request_logs 全量采集字段（正文/标头/七维 token/请求 ID/**来源 client**）与**两阶段落库**（startLog 落 pending 行 → recordLog 按 request_id upsert 回填 / 启动收口残留为 499）、**日志二级保留（2026-09-19）**：行保留最近 7 天、正文（`request_body` / `response_body`，体积大头）只留 12~24 小时、每次清扫按 id 分批推进不阻塞主进程（**2026-09-23 起改由 `log:retention` / `usage:retention` / `db:wal-checkpoint` 定时任务驱动，见 app/13**）、以及「删除不缩文件、需要时用 `VACUUM` 缩容（WAL 已每日自动 checkpoint）」的操作指引、用量双粒度聚合表（日永久 + 小时 7 天）与 7 类统计口径、**归档 / 改名与历史数据**（名字快照语义：归档不影响历史、`archived_at` 状态位 + 级联归档 / 恢复只恢复自身 / 名字不释放、为什么从物理删除改成归档、改名按 provider_id/model_id 集合式改写历史 + 性能边界与已知限制）、repo 与 IPC 通道清单（含 log:changed 推送与 quota 域）、@common/types 三端契约组织、新增表流程备忘 |

### server/ —— 本地代理服务

| 文档                                             | 描述                                                                                                             |
|--------------------------------------------------|------------------------------------------------------------------------------------------------------------------|
| [01-本地代理服务.md](./server/01-本地代理服务.md) | 本地代理（127.0.0.1，**2026-09-19 起三协议入口**）：生命周期（启动/保存重启/退出）、鉴权（Bearer + x-api-key）、显式路由（/v1/models + 三条协议入口 /v1/chat/completions、/v1/messages、/v1/responses；均按归档状态过滤，命中归档模型回 404 `model_archived`）、模型映射与转发分发（同协议 raw 透传 / 异协议转换）、SSE 流式管道与 usage（含明细）提取、请求/响应正文与标头全量采集（**线上口径**；两阶段落库：pending 行 → upsert 回填；脱敏 + 兜底估算）、**来源客户端归一**：`parseClientName(user-agent)` 落 `client` 列、log:changed 实时推送（300ms 合并广播）、错误表（全局拦截 OpenAI 信封 + 路由内按入口协议信封）、出站统一 axios（upstream/httpClient.ts，网络错误增强 + CONNECT 隧道代理）、目录：每路由一文件（routes/entry.ts 工厂）+ app.ts 组装 + strategies/ 双向编解码 + upstream/logging 分域 |
| [02-协议适配与转换.md](./server/02-协议适配与转换.md) | **策略 = 双向编解码器（2026-09-19 二次重构，可入可出）**：3×3 协议矩阵（三入口 × 三上游，对角线 raw 透传保未知字段，异协议经统一会话转换）、`ProtocolStrategy` 双向接口（parse/encode 入口向 + request/decode 上游向 + 透传传输特征）、统一会话/流事件/完成结果（conversation.ts 中性枢纽）、forward 分发 + passthrough（usage 经解码器抽头）+ converted（真·非流式）、每协议五件套（parse/encode/request/decode/index）、各协议逐字段映射表、错误信封规则（全局 OpenAI + 路由内按入口协议）、usage 口径（Anthropic prompt 含缓存读写）与限制清单 |
